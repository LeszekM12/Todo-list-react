import styled from "styled-components";

export const StyledHeader = styled.h1`
        font-weight: bold;
        max-width: 700px;

        @media(max-width: 767px) {    
            text-align: center;
        }   
`;